# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target '8 Ball of Love' do
  pod "DHCShakeNotifier"

  target '8 Ball of LoveTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
